{% block body %}

    <p>
        <a href="{{ path('app_pizza_new') }}">New pizza</a>
    </p>

    <ul>
        {% for entity in entities %}
            <li>{{ entity }}</li>
        {% endfor %}
    </ul>

{% endblock %}
